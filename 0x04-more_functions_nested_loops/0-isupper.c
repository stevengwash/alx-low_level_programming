#include "main.h"
/**
 * -isupper checking for uppercase programme
 * @c: variable text 
 * return  always 0.
 */
int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else 
{
return (0);
}
}
