#include "main.h"
/**
 *  * mul funtion checking for digits (0 through).
 *   * a@ a - variable
 *    * @b b - variable
 *     * return: always 0.
 *      */
int mul(int a, int b)
{
	        int mul;

		        mul = a * b;
			        return (mul);
}

